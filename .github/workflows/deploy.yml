name: Legacy EC2 Deploy

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest

    steps:
      - name: Repository now deploys with ECS
        run: |
          echo "The EC2 deployment workflow has been retired."
          echo "Use .github/workflows/aws-ecs-deploy.yml for ECR + ECS Fargate deployments."
